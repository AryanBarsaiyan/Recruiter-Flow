# Used by EnvConfigImportTest to verify spring.config.import loads properties.
ENV_LOADED=yes
TEST_PROP=from-env-test
# Disable rate limiting in tests so integration tests don't get 429
APP_RATE_LIMIT_ENABLED=false
